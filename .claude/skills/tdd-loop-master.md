# TDD Loop Master

**Tags:** [TDD, Testing, Red-Green]

Strict TDD process:
1. Write a failing test that defines the desired behavior.
2. Write the minimal code to make the test pass.
3. Refactor while keeping tests green.

Never write implementation before tests. This prevents "test laundering" where tests are adjusted to match buggy code.
